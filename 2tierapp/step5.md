Run command to create a mysql deployment
`kubectl apply -f mysql-deployment.yaml`{{execute}}


Run command to get details about the service, deployment and the pods
`kubectl get svc,deploy,pods`{{execute}}


Run command to get details about the service, deployment and the pods
`kubectl get svc,deploy,pods`{{execute}}
